# SOPS private key — never committed
*.agekey
*.age
.keys/

# Plaintext env files — only .env.sops (encrypted) goes in repo
**/.env
!**/.env.example
!**/.env.sops
/AGENTS.md

# Local agent tooling (opencode skills/config) — never committed
/.opencode/
