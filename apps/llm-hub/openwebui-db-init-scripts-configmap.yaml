---
# ConfigMap with post-init template SQL for the Open WebUI application database.
# Creates the pgvector extension in the template database so app users can use it.
apiVersion: v1
kind: ConfigMap
metadata:
  name: openwebui-db-init-scripts
  namespace: llm-hub
data:
  init-template.sql: |
    CREATE EXTENSION IF NOT EXISTS vector;
