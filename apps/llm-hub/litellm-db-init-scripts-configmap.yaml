---
# ConfigMap with post-init template SQL for the litellm application database.
# Creates the pgvector extension in the template database so app users can use it.
apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-db-init-scripts
  namespace: llm-hub
data:
  init-template.sql: |
    CREATE EXTENSION IF NOT EXISTS vector;
