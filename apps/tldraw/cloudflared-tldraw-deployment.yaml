---
# Dedicated Cloudflare Tunnel for white-board.voitech.dev -> tldraw server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-tldraw
  namespace: apps-tldraw
  labels:
    app.kubernetes.io/name: cloudflared-tldraw
    app.kubernetes.io/component: tunnel
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared-tldraw
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflared-tldraw
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - $(TUNNEL_TOKEN)
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: tldraw-tunnel-token
                  key: token
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
