---
apiVersion: v1
kind: Service
metadata:
  name: speaches
  namespace: voice
  labels:
    app.kubernetes.io/name: speaches
    app.kubernetes.io/component: stt
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: speaches
