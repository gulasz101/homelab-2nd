---
apiVersion: v1
kind: Service
metadata:
  name: sonarr-nodeport
  namespace: arr-stack
spec:
  selector:
    app.kubernetes.io/name: sonarr
  ports:
    - name: http
      port: 8989
      targetPort: http
      nodePort: 30089
  type: NodePort
