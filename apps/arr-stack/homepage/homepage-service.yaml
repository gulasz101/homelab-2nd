---
apiVersion: v1
kind: Service
metadata:
  name: homepage-nodeport
  namespace: arr-stack
spec:
  selector:
    app.kubernetes.io/name: homepage
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30030
  type: NodePort
