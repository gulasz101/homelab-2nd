apiVersion: v1
kind: Namespace
metadata:
  name: openchamber
  labels:
    app.kubernetes.io/part-of: homelab
    # OTel Collector DaemonSet ships container logs to Loki (guardrail 9).
    observability.giantswarm.io/tenant: homelab
