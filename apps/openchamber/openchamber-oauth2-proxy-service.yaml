---
apiVersion: v1
kind: Service
metadata:
  name: openchamber-oauth2-proxy
  namespace: openchamber
  labels:
    app.kubernetes.io/name: openchamber-oauth2-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openchamber-oauth2-proxy
  ports:
    - name: http
      port: 4180
      targetPort: 4180
      protocol: TCP
