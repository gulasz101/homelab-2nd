---
# Honcho Deriver worker Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: honcho-deriver
  namespace: honcho
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: honcho-deriver
  template:
    metadata:
      labels:
        app.kubernetes.io/name: honcho-deriver
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: deriver
          # Pinned to the digest currently running in production. ghcr.io only
          # has a "latest" tag for this image, so digest pinning is the only
          # way to prevent surprise upstream changes on pod restart.
          image: ghcr.io/gulasz101/honcho@sha256:c027a8fb333323e62393638fd0db83f5a7fb2452afe296fd1afb93a7e5fd36af
          command: ["python", "-m", "src.deriver"]
          workingDir: /app
          env:
            - name: DB_CONNECTION_URI
              valueFrom:
                secretKeyRef:
                  name: honcho-db-credentials
                  key: connection-uri
            - name: CACHE_URL
              valueFrom:
                secretKeyRef:
                  name: honcho-redis-credentials
                  key: cache-url
            - name: LLM_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: honcho-litellm-key
                  key: api-key
          envFrom:
            - configMapRef:
                name: honcho-config
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
