# MinIO credentials template — copy to .env and fill in real values
# .env is gitignored; the real .env is SOPS-encrypted as .env.sops for backup
MINIO_ROOT_USER=changeme
MINIO_ROOT_PASSWORD=changeme
