---
# ServiceAccount required by the NVIDIA device plugin chart when an
# external ConfigMap is referenced via config.name.
# The chart only auto-creates this SA when config.map is used.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nvidia-device-plugin-service-account
  namespace: nvidia-device-plugin
