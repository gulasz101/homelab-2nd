---
# PVC for Redis data (rebuildable cache; local-path for performance)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: auth
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
