---
# Dedicated Cloudflare Tunnel for auth.voitech.dev -> Authentik server
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-auth
  namespace: auth
  labels:
    app.kubernetes.io/name: cloudflared-auth
    app.kubernetes.io/component: tunnel
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared-auth
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflared-auth
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --no-autoupdate
            - run
            - --token
            - $(TUNNEL_TOKEN)
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: auth-tunnel-token
                  key: token
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
